<h2>Welcome</h2>
@if (User.Identity.IsAuthenticated)
{
    <p>You are logged in as @User.Identity.Name (role: @User.FindFirst(System.Security.Claims.ClaimTypes.Role)?.Value).</p>
}
else
{
    <p><a href="/Account/Login">Please log in</a>.</p>
}